---
title: "Quickstart"
description: "Run Better Auth Docs Discord Bot with the current scripts and configuration."
order: 10
---

# Quickstart

This is the shortest path from clone to a useful first run of Better Auth Docs Discord Bot. The goal is not to understand every file yet; the goal is to install dependencies, provide required configuration, and run the command that proves the project is alive.
## What You Will Accomplish
- Prepare the project with the detected package manager.
- Add the minimum environment configuration needed for runtime behavior.
- Run the primary workflow so you can confirm the project is usable before exploring advanced docs.
## Before You Start
This project uses `bun` as its package manager.
Environment-backed behavior is present, so configuration is part of the first-run path.
## Recommended First Run
1. Install dependencies with `bun install`.
2. Copy `env.example` to `.env` and set the required values.
3. Run `bun run start` to exercise the main workflow.
## Useful Commands
These commands are useful after the first run because they are the repeatable workflows available in the project.
- `build` runs `tsc` and creates the production build or compiled package artifacts.
- `start` runs `node dist/index.js` and starts the runtime entrypoint after dependencies or build output are ready.
- `dev` runs `ts-node src/index.ts` and starts the local development workflow.
## Useful CLI Options
No CLI options were found in the current source entrypoints.
## Next Places to Read
- Better Auth Docs Discord Bot: `docs/page.mdx`
- Installation: `docs/installation/page.mdx`
- Configuration: `docs/configuration/page.mdx`
- Environment Variables: `docs/configuration/environment/page.mdx`
- API Reference: `docs/api/page.mdx`
- Public Exports: `docs/api/exports/page.mdx`
